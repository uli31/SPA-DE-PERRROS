import React,{useState,useContext} from 'react'
import Prueba from './Prueba'
import { UsuarioContex } from '../context/usuario-context';

export default function Pedidos(listaTareas) {





   
     
    return (

        <>{listaTareas.map(tareas=>{
            return(
                <p>hola</p>
            )
        })}
        
        </>
    )
}
